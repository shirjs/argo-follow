apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-app-1730278468
  namespace: shir1
  annotations:
    app.kubernetes.io/instance: flask-app-1730278468
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flask-app-1730278468
  template:
    metadata:
      labels:
        app: flask-app-1730278468
        developer: shir1
    spec:
      containers:
      - name: flask-app
        image: shirbenyosef1/flask_app
        ports:
        - containerPort: 5000
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: flask-app-1730278468
  namespace: shir1
spec:
  type: NodePort
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 32713  # Specify the nodePort explicitly
  selector:
    app: flask-app-1730278468
